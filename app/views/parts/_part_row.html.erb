<div class="well">

  <div class="row">
    <div class="col-xs-8">
      <h4><%= part.name %></small></h4>
    </div>
    <div class="col-xs-4">

      <%= form_for order_item, remote: true do |f| %>
      <h4 class="text-right">Unit Price: <span style="color: green"><%= number_to_currency part.price %></span></h4>
        <div class="input-group">
          <%= f.number_field :quantity, value: 0, class: "form-control", min: 1 %>
          <div class="input-group-btn">
            <%= f.hidden_field :part_id, value: part.id %>
            <%= f.submit "Add to Cart", class: "btn btn-primary" %>
          </div>
        </div>
      <% end %>
    </div>

  </div>
</div>
