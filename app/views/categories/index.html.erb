<br>Categories<br>
<%=link_to "Add parts to a category", new_part_path(@part)%><br>
<% @categories.sort_by(&:name).each do |category| %>
<%=category.id%> <%=category.name.capitalize%><br>

<%= link_to "edit", edit_category_path(id: category.id) %> <%= link_to 'delete',category_path(id: category.id),
method: :delete, data: { confirm: "Are you sure?"} %><br><br>
<%end%>
